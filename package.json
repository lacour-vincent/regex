{
  "name": "regex",
  "version": "1.0.0",
  "description": "A regular expression is a sequence of characters that define a search pattern",
  "scripts": {
    "type-check": "tsc --noEmit",
    "format-check": "prettier --check \"**/src/**/*.ts\"",
    "validate": "yarn run type-check && yarn run format-check",
    "test": "vitest run --config=vitest.config.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2",
    "vitest": "^0.34.4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lacour-vincent/regex.git"
  },
  "license": "MIT"
}
